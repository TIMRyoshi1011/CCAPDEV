<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>All Reviews</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="style.css">
    <!-- <link rel="stylesheet" href="feed.css"> -->
</head>

<body>
    <!-- Header (same as homepage, might need to change if needed account before accessing reviews) -->
    <div class="header">
        <h3 class="name" onclick="window.location.href='/'" style="cursor: pointer;">Bite<span>Back</span></h3>

        <div class="buttons">
            <button class="login" onclick="openLoginPopup()">Login</button>
            <button class="sign-up" onclick="openSignUpPopup()">Sign Up</button>
        </div>
    </div>

    <!-- Login Popup -->
    <div id="loginPopup" class="popup hidden">
        <div class="popup-content">
            <span class="close" onclick="closeLoginPopup()">&times;</span>
            <h2>Sign in to Bite<span>Back</span></h2>
            <h1>Please Enter your Login Details Below</h1>
            <form id="loginForm">
                <div class="form-group">
                    <label for="login-email">Email</label>
                    <input type="email" id="login-email" required>
                </div>
                <div class="form-group">
                    <label for="login-password">Password</label>
                    <input type="password" id="login-password" required>
                </div>

                <div id="login-error-msg" class="form-error-message"></div>

                <div class="login-options">
                    <label class="remember-me">
                        <input type="checkbox"> Remember me
                    </label>
                    <a href="#" class="forgot-password" onclick="openForgotPasswordPopup(); return false;">Forgot
                        password?</a>
                </div>
                <button type="submit" class="submit-btn">Login</button>
            </form>

            <p class="popup-footer">Don't have an account? <a href="#" onclick="switchToSignUp()">Sign up here</a></p>
        </div>
    </div>

    <!--Forgot Password Popup-->
    <div id="forgotPasswordPopup" class="popup hidden">
        <div class="popup-content">
            <span class="close" onclick="closeForgotPasswordPopup()">&times;</span>
            <h2>Forgot Password?</h2>
            <h1>Please enter your email address to reset your password</h1>

            <form onsubmit="submitForgotPassword(); return false;">
                <div class="form-group">
                    <label for="forgot-email">Email</label>
                    <input type="email" id="forgot-email" placeholder="example.name@domain.com" required>
                </div>

                <div id="forgot-error-msg" class="form-error-message"></div>

                <button type="submit" class="submit-btn">Submit</button>
            </form>

            <p class="popup-footer">
                <a href="#" onclick="closeForgotPasswordPopup(); openLoginPopup(); return false;">Back to Login</a>
            </p>
        </div>
    </div>

    <!-- Change Password Popup -->
    <div id="changePasswordPopup" class="popup hidden">
        <div class="popup-content">
            <span class="close" onclick="closeChangePassword()">&times;</span>
            <h2>Change Password</h2>
            <h1>Please enter your new password below</h1>

            <form onsubmit="event.preventDefault(); submitChangePassword();">
                <div class="form-group">
                    <label for="email-code">Code sent on your email</label>
                    <input type="password" id="email-code">
                </div>

                <div class="form-group">
                    <label for="new-password">New Password</label>
                    <input type="password" id="new-password" required>
                </div>

                <div class="form-group">
                    <label for="confirm-password">Confirm New Password</label>
                    <input type="password" id="confirm-password" required>
                </div>

                <div id="change-pw-error-msg" class="form-error-message"></div>

                <button type="submit" class="submit-btn">Change Password</button>
            </form>
        </div>
    </div>

    <!-- Sign Up Popup -->
    <div id="signupPopup" class="popup hidden">
        <div class="popup-content">
            <span class="close" onclick="closeSignUpPopup()">&times;</span>
            <h2 style="text-align: center;">Register for your own<span style="display:block;">Bite<span>Back</span>
                    Account</span>
            </h2>
            <form id="signupForm">
                <div class="form-group">
                    <label for="signup-name">Full Name</label>
                    <input type="text" id="signup-name" required>
                </div>
                <div class="form-group">
                    <label for="signup-email">Email</label>
                    <input type="email" id="signup-email" placeholder="example.name@domain.com" required>
                </div>
                <div class="form-group">
                    <label for="signup-password">Enter password</label>
                    <input type="password" id="signup-password" required>
                </div>
                <div class="form-group">
                    <label for="signup-confirm">Re-enter password</label>
                    <input type="password" id="signup-confirm" required>
                </div>

                <div id="signup-error-msg" class="form-error-message"></div>

                <button type="submit" class="submit-btn">Register Account</button>
            </form>

            <p class="popup-footer">Already have an account? <a href="#" onclick="switchToLogin()">Login</a></p>
        </div>
    </div>

    <!-- Account Created Popup -->
    <div id="accountCreatedPopup" class="popup hidden">
        <div class="popup-content popup-success">
            <span class="close" onclick="closeAccountSuccess()">&times;</span>
            <h2>Hello <span id="accountName"></span>!</h2>
            <h1 class="success-welcome">Welcome to <span>Bite</span>Back</h1>
            <p class="success-subtitle">Your account has been created successfully.</p>
            <button class="submit-btn" onclick="goToFeed()">Go to Feed</button>
        </div>
    </div>

    <!-- Page Title -->
    <section class="main">
        <h1>Community Reviews</h1>
        <p>Real experiences from real foodies around Metro Manila</p>
    </section>

    <section class="top-reviews">
        <div class="top-header">
            <div>
                <h2>All Reviews</h2>
                <p>Browse restaurants by taste, service, and vibe</p>
            </div>
        </div>

        <!-- Reviews -->
        <div class="review-grid-2">
            {{#each posts}}
            <div class="review-card-2" data-user-vote="{{userVote}}">
                <div class="review-header">
                    <h3>{{restaurant}}</h3>
                </div>

                <div class="review-user-2">
                    <div class="review-avatar {{currentUser.rankClass}}">{{currentUser.avatar}}</div>
                    <div class="review-user-info">
                        <div>
                            <highlight>{{currentUser.name}}</highlight>
                            <span class="badge {{currentUser.rankClass}}">{{currentUser.badge}}</span>
                            {{#if currentUser.verified}}
                            <span class="verified-2">✔ Verified</span>
                            {{/if}}
                        </div>
                        <div class="review-date">{{formatDate date}}</div>
                    </div>
                </div>

                <div class="review-meta-row" style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 15px; margin-top: 10px;">
                    <div class="review-meta-left" style="display: flex; gap: 10px;">
                        {{#if cuisine}}
                        <span class="badge-cuisine">🍽️ {{cuisine}}</span>
                        {{/if}}
                        {{#if location}}
                        <span class="badge-location">📍 {{location}}</span>
                        {{/if}}
                    </div>
                    <div class="star-rating">
                        {{{renderStars ratingValue}}}
                        <span class="rating-number">{{ratingValue}}</span>
                    </div>
                </div>

                <div class="review-scores">
                    <div class="score-item">
                        <span class="score-label">Service:</span>
                        <div class="star-rating small">
                            {{{renderStars scores.service}}}
                        </div>
                    </div>
                    <div class="score-item">
                        <span class="score-label">Taste:</span>
                        <div class="star-rating small">
                            {{{renderStars scores.taste}}}
                        </div>
                    </div>
                    <div class="score-item">
                        <span class="score-label">Ambiance:</span>
                        <div class="star-rating small">
                            {{{renderStars scores.ambiance}}}
                        </div>
                    </div>
                </div>

                <div class="review-title-2">{{title}}</div>
                <div class="review-text-2">{{content}}</div>


            </div>
            {{/each}}
        </div>
    </section>

    <!-- Global Alert Popup -->
    <div id="customAlertPopup" class="popup hidden">
        <div class="popup-content" style="text-align: center;">
            <span class="close" onclick="closeCustomAlert()">&times;</span>
            <div class="success-icon-container">
                <span>✓</span>
            </div>
            <h2 class="success-title">Notice</h2>
            <p id="customAlertMessage" class="success-message"></p>
            <button class="success-done-btn" onclick="closeCustomAlert()">OK</button>
        </div>
    </div>

    <!-- Confirm Popup -->
    <div id="customConfirmPopup" class="popup hidden">
        <div class="popup-content" style="text-align: center;">
            <span class="close" onclick="closeCustomConfirm(false)">&times;</span>
            <div class="success-icon-container"
                style="background-color: #f59e0b; box-shadow: 0 4px 12px rgba(245, 158, 11, 0.3);">
                <span>?</span>
            </div>
            <h2 class="success-title">Confirm</h2>
            <p id="customConfirmMessage" class="success-message"></p>
            <div style="display: flex; justify-content: center; gap: 12px; margin-top: 20px;">
                <button class="success-done-btn" onclick="closeCustomConfirm(true)"
                    style="width: 120px; background-color: #d94866;">Yes</button>
                <button class="success-done-btn" onclick="closeCustomConfirm(false)"
                    style="width: 120px; background-color: #f3f4f6; color: #4b5563; box-shadow: none;">Cancel</button>
            </div>
        </div>
    </div>

    <!-- Report Post Popup -->
    <div id="reportReviewPopup" class="popup hidden">
        <div class="popup-content">
            <span class="close" onclick="closeReportPopup()">&times;</span>
            <div class="report-header">
                <h2 class="report-title">
                    <span class="report-title-icon">🚩</span> Report this post?
                </h2>
                <p class="report-description">This will flag the post for review. Please tell us why.</p>
            </div>

            <div class="report-textarea-group">
                <textarea id="report-reason" class="report-textarea" rows="3" placeholder="Reason for reporting..."
                    required></textarea>
            </div>

            <div id="report-error-msg" class="form-error-message"></div>

            <div class="report-actions">
                <button class="submit-btn report-cancel-btn" onclick="closeReportPopup()">Cancel</button>
                <button class="submit-btn report-submit-btn" onclick="submitReport()">Report</button>
            </div>
        </div>
    </div>

    <!-- Report Success Popup -->
    <div id="reportSuccessPopup" class="popup hidden">
        <div class="popup-content" style="text-align: center;">
            <span class="close" onclick="closeReportSuccessPopup()">&times;</span>
            <div class="success-icon-container"
                style="background-color: #d97706; box-shadow: 0 4px 12px rgba(217, 119, 6, 0.3);">
                <span style="font-size: 28px;">🚩</span>
            </div>
            <h2 class="success-title">Report Submitted</h2>
            <p class="success-message" style="margin-bottom: 24px;">Thank you for helping keep our community safe. We'll
                review this post and take appropriate action.</p>
            <button class="success-done-btn" onclick="closeReportSuccessPopup()">Done</button>
        </div>
    </div>

    <script src="script.js"></script>
    </div>

</body>

</html>